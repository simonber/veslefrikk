#include <iostream>
#include "chilleFunksjoner.h"
#include <string>

int main() {
	//std::string meld = tempWarning(70, 20, 20, -15);
	//std::cout << meld << meld.length() << std::endl;

	int P_sample = 0;
	int WL_sample = 0;
	const int ORDER_WL = 50;
	const int ORDER_P = 4;
	int P_buff[ORDER_P] = {};
	int WL_buff[ORDER_WL] = {};
	while (P_sample != -1) {

		std::cout << "Skriv inn pumpesample: ";
		std::cin >> P_sample;
		std::cout << std::endl;
		samplePump(P_buff, P_sample);

		std::cout << "Skriv inn vannstandsample: ";
		std::cin >> WL_sample;
		std::cout << std::endl;
		WL_buff[countWL] = WL_sample;
		
		for (int i = 0; i < ORDER_P; i++) {
			std::cout << i << ": " << P_buff[i] << std::endl;
		}
		waterLevelCheck(WL_buff);
		gotThePump(P_buff, WL_buff);
		std::cout << msg << std::endl;
		countWL++;
	}

	
	return 0;
}
